# Origin Validation Configuration Example
# Set this environment variable to enable origin validation

# Example: Allow requests from your auth service
ALLOWED_ORIGINS=http://localhost:3000,https://your-auth-service.com

# Multiple origins example:
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,https://your-domain.com

# If not set, origin validation is disabled (all requests allowed)
